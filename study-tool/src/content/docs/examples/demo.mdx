---
title: "Linear Algebra: Eigenvalues"
description: Demo note showing LaTeX, code, flashcards, and quizzes.
---

import Flashcard from '../../../components/Flashcard';
import Quiz from '../../../components/Quiz';

## Definition

An **eigenvalue** $\lambda$ of a matrix $A$ is a scalar such that

$$
A\mathbf{v} = \lambda \mathbf{v}
$$

for some nonzero vector $\mathbf{v}$, called the **eigenvector**.

## Finding Eigenvalues

We solve the **characteristic equation**:

$$
\det(A - \lambda I) = 0
$$

### Example

Let $A = \begin{pmatrix} 4 & 1 \\ 2 & 3 \end{pmatrix}$.

$$
\det\begin{pmatrix} 4 - \lambda & 1 \\ 2 & 3 - \lambda \end{pmatrix} = (4-\lambda)(3-\lambda) - 2 = \lambda^2 - 7\lambda + 10 = 0
$$

So $\lambda_1 = 5$, $\lambda_2 = 2$.

## Computing with NumPy

```python
import numpy as np

A = np.array([[4, 1],
              [2, 3]])

eigenvalues, eigenvectors = np.linalg.eig(A)
print(f"Eigenvalues: {eigenvalues}")
print(f"Eigenvectors:\n{eigenvectors}")
```

```
Eigenvalues: [5. 2.]
Eigenvectors:
[[ 0.70710678 -0.4472136 ]
 [ 0.70710678  0.89442719]]
```

## Key Properties

- The **trace** of $A$ equals the sum of eigenvalues: $\text{tr}(A) = \sum \lambda_i$
- The **determinant** equals the product: $\det(A) = \prod \lambda_i$
- A matrix is **invertible** iff no eigenvalue is zero
- **Symmetric matrices** have real eigenvalues and orthogonal eigenvectors


## Flashcards

<Flashcard client:load cards={[
  {
    front: "What is the characteristic equation of a matrix $A$?",
    back: "$\\det(A - \\lambda I) = 0$"
  },
  {
    front: "How does $\\text{tr}(A)$ relate to eigenvalues?",
    back: "The trace equals the sum of all eigenvalues: $\\text{tr}(A) = \\sum \\lambda_i$"
  },
  {
    front: "When is a matrix diagonalisable?",
    back: "When it has $n$ linearly independent eigenvectors (equivalently, when geometric multiplicity equals algebraic multiplicity for each eigenvalue)."
  },
  {
    front: "What is special about eigenvalues of symmetric matrices?",
    back: "They are always real, and the eigenvectors are orthogonal (spectral theorem)."
  },
  {
    front: "If $\\lambda$ is an eigenvalue of $A$, what are the eigenvalues of $A^{-1}$?",
    back: "$1/\\lambda$ (with the same eigenvectors)"
  }
]} />


## Quiz

<Quiz client:load title="Test Your Knowledge" questions={[
  {
    question: "What are the eigenvalues of $I_n$ (the $n \\times n$ identity matrix)?",
    options: ["$0$ with multiplicity $n$", "$1$ with multiplicity $n$", "$1$ and $-1$", "$n$"],
    answer: 1,
    explanation: "Every vector is an eigenvector of $I_n$ with eigenvalue $1$, since $I\\mathbf{v} = 1 \\cdot \\mathbf{v}$."
  },
  {
    question: "If $A$ is $3 \\times 3$ with eigenvalues $1, 2, 3$, what is $\\det(A)$?",
    options: ["$6$", "$5$", "$3$", "$9$"],
    answer: 0,
    explanation: "The determinant is the product of eigenvalues: $1 \\times 2 \\times 3 = 6$."
  },
  {
    question: "A matrix has eigenvalue $0$. What can you conclude?",
    options: [
      "The matrix is symmetric",
      "The matrix is not invertible",
      "The matrix is the zero matrix",
      "The eigenvector is the zero vector"
    ],
    answer: 1,
    explanation: "If $\\lambda = 0$ then $\\det(A) = 0$ (since $\\det A = \\prod \\lambda_i$), so $A$ is singular."
  },
  {
    question: "Which decomposition uses eigenvalues and eigenvectors?",
    options: ["LU decomposition", "QR decomposition", "Eigendecomposition $A = PDP^{-1}$", "Cholesky decomposition"],
    answer: 2,
    explanation: "$P$ has eigenvectors as columns and $D$ is diagonal with eigenvalues."
  }
]} />


## Further Reading

- Gilbert Strang, *Introduction to Linear Algebra*, Chapter 6
- 3Blue1Brown: [Eigenvectors and Eigenvalues](https://www.youtube.com/watch?v=PFDu9oVAE1g)
